SELECT 
	CONCAT(first_name, ' ',last_name) AS 'nombre completo'
FROM employees
-- WHERE emp_no = '10001' 
-- esto es un comentario
-- emp_no, salary, from_date, (salary * 3) + 10 AS 'AUMENTO DE SUELDO', 